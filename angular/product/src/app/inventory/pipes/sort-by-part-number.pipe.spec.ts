import { SortByPartNumberPipe } from './sort-by-part-number.pipe';

describe('SortByPartNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByPartNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
